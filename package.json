{
  "name": "codex-subscription-router-patcher",
  "private": true,
  "version": "0.1.0",
  "description": "Build tooling for the independent Codex Subscription Router desktop patch",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/b-nnett/codex-subscription-router.git"
  },
  "homepage": "https://github.com/b-nnett/codex-subscription-router#readme",
  "bugs": {
    "url": "https://github.com/b-nnett/codex-subscription-router/issues"
  },
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "check": "npm run check:go && npm run check:js && npm run check:python && npm run check:shell",
    "check:go": "go test ./... && go vet ./...",
    "check:js": "node --check ui/account-menu.js && node --check ui/thread-subscription.js && node --check ui/ui-test-bridge.cjs",
    "check:python": "python3 -m compileall -q scripts",
    "check:python:tests": "python3 -m unittest discover -s scripts/tests -p \"test_*.py\"",
    "check:shell": "bash -n install.sh",
    "release:check": "python3 scripts/check_release.py"
  },
  "devDependencies": {
    "@electron/asar": "4.2.1"
  }
}
